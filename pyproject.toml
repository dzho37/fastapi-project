[project]
name = "fastapi-project"
version = "0.1.0"
description = "project for study"
readme = "README.md"
authors = [
    { name = "Zhomart Zhumabekov", email = "dzhocorp@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
fastapi-project = "fastapi_project:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
